# TownHawll Web

The public and authenticated user-facing Next.js application.

Run it from the repository root with pnpm --filter @townhawll/web dev.

First-time users finish two onboarding steps at `/onboarding`. Games and Screen
landing routes are not built yet, so those focus selections temporarily finish
at `/`; the saved content-focus value still determines their future default.
